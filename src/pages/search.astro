---
import Base from "@layouts/Base.astro";
import SearchPage from "@components/SearchPage"
const metadata = {
  title: "SuperOffice Docs",
};

---

<Base scroll={false} lang="en" metadata={metadata}>
    <SearchPage client:load/>
</Base>


